<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم - ZIN CLEAN</title>
    
    <!-- Google Fonts: Cairo -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;700;900&display=swap" rel="stylesheet">
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="style.css"> <!-- نفس ملف CSS العام -->

    <script>
        document.documentElement.style.backgroundColor = '#020617';
    </script>

    <style>
        / Admin specific styles to keep CSS file clean /
        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }
        
        / Sidebar /
        .admin-sidebar {
            width: 280px;
            background: #0f172a;
            border-left: 1px solid #1e293b;
            position: fixed;
            height: 100vh;
            display: flex;
            flex-direction: column;
            padding: 20px;
            transition: transform 0.3s ease;
            z-index: 100;
        }

        .admin-sidebar.hidden {
            transform: translateX(100%); / RTL: Move to right /
        }

        .admin-content {
            flex: 1;
            margin-right: 280px; / RTL: Sidebar is on right /
            padding: 20px;
            transition: margin-right 0.3s ease;
            width: calc(100% - 280px);
        }

        .menu-toggle {
            display: none;
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .admin-sidebar {
                transform: translateX(100%);
            }
            .admin-sidebar.active {
                transform: translateX(0);
            }
            .admin-content {
                margin-right: 0;
                width: 100%;
            }
            .menu-toggle {
                display: block;
                position: fixed;
                top: 15px;
                right: 15px;
                z-index: 200;
            }
        }

        / Dashboard Cards /
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: #1e293b;
            padding: 20px;
            border-radius: 12px;
            border: 1px solid #334155;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .stat-icon {
            width: 50px;
            height: 50px;
            background: rgba(37, 99, 235, 0.2);
            color: #2563eb;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }

        / Tables /
        .table-container {
            background: #1e293b;
            border-radius: 12px;
            padding: 20px;
            overflow-x: auto;
            border: 1px solid #334155;
            margin-bottom: 30px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px 15px;
            text-align: right;
            border-bottom: 1px solid #334155;
        }

        th {
            color: #94a3b8;
            font-weight: 600;
        }

        tr:hover {
            background: #1e293b; / Slightly lighter than container bg /
        }

        .status-badge {
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
        }
        
        .status-pending { background: #fbbf24; color: #78350f; }
        .status-approved { background: #22c55e; color: #14532d; }
        .status-rejected { background: #ef4444; color: #450a0a; }

        .btn-sm {
            padding: 5px 10px;
            font-size: 0.8rem;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            margin-left: 5px;
        }

        .btn-delete {
            background: #ef4444;
            color: white;
        }
    </style>
</head>
<body>

    <button class="menu-toggle" id="toggleSidebar">
        <i class="fa-solid fa-arrow-right-from-bracket"></i>
    </button>

    <!-- Sidebar -->
    <aside class="admin-sidebar" id="sidebar">
        <div class="logo text-xl font-bold">
            <i class="fa-solid fa-sparkles text-blue-500"></i>
            <span> ZIN ADMIN</span>
        </div>
        <nav style="margin-top: 40px;">
            <ul style="list-style: none; padding: 0;">
                <li style="margin-bottom: 10px;">
                    <a href="#" class="active-menu">
                        <i class="fa-solid fa-gauge"></i>
                        <span>لوحة القيادة</span>
                    </a>
                </li>
                <li style="margin-bottom: 10px;">
                    <a href="#requests">
                        <i class="fa-solid fa-list"></i>
                        <span>الطلبات الواردة</span>
                    </a>
                </li>
                <li style="margin-bottom: 10px;">
                    <a href="#workers">
                        <i class="fa-solid fa-users"></i>
                        <span>قائمة العمالة</span>
                    </a>
                </li>
                <li style="margin-top: auto;">
                    <button onclick="clearAllData()" style="width: 100%; padding: 10px; background: #7f1d1d; color: white; border: none; border-radius: 5px; cursor: pointer;">
                        مسح البيانات
                    </button>
                </li>
            </ul>
        </nav>
    </aside>

    <!-- Main Content -->
    <main class="admin-content">
        <!-- Header -->
        <header style="margin-bottom: 30px; display: flex; justify-content: space-between; align-items: center;">
            <h2 id="page-title">لوحة القيادة</h2>
            <div style="display: flex; gap: 10px;">
                <div class="stats-widget flex-center">
                    <span style="color: #94a3b8;">المستخدمين:</span>
                    <strong id="total-orders">0</strong>
                </div>
            </div>
        </header>

        <!-- Stats Dashboard -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon"><i class="fa-solid fa-file-invoice"></i></div>
                <div>
                    <div style="color: #94a3b8;">إجمالي الطلبات</div>
                    <h3 id="stat-orders">0</h3>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fa-solid fa-user-clock"></i></div>
                <div>
                    <div style="color: #94a3b8;">تحت المراجعة</div>
                    <h3 id="stat-pending">0</h3>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fa-solid fa-users"></i></div>
                <div>
                    <div style="color: #94a3b8;">عدد الموظفين</div>
                    <h3 id="stat-workers">0</h3>
                </div>
            </div>
        </div>

        <!-- Orders Section -->
        <div id="requests" class="table-container">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px;">
                <h3>أحدث الطلبات</h3>
                <button onclick="toggleWorkerMode()" id="modeBtn" class="btn-primary">تبديل الوضع (طلب/عامل)</button>
            </div>
            <div id="orders-list">
                <!-- Dynamic Content -->
            </div>
        </div>

        <!-- Workers Management Section -->
        <div id="workers" class="table-container" style="display: none;">
            <div style="margin-bottom: 15px;">
                <h3>إدارة العمالة</h3>
            </div>
            
            <form id="addWorkerForm" class="flex-row" style="gap: 10px; margin-bottom: 20px; flex-wrap: wrap;">
                <input type="text" id="workerName" placeholder="اسم الموظفة" style="flex: 1; padding: 10px; border-radius: 6px; border: 1px solid #475569; background: #334155; color: white;" required>
                <input type="text" id="workerPhone" placeholder="رقم التليفون" style="flex: 1; padding: 10px; border-radius: 6px; border: 1px solid #475569; background: #334155; color: white;" required>
                <input type="text" id="workerID" placeholder="رقم البطاقة الوطنية" style="flex: 2; padding: 10px; border-radius: 6px; border: 1px solid #475569; background: #334155; color: white;" required>
                <select id="workerStatus" style="flex: 0; padding: 10px; border-radius: 6px; border: 1px solid #475569; background: #334155; color: white;">
                    <option value="active">نشط</option>
                    <option value="suspended">معلق (حالة)</option>
                </select>
                <button type="submit" class="btn-primary">إضافة</button>
            </form>

            <table id="workers-table">
                <thead>
                    <tr>
                        <th>الاسم</th>
                        <th>رقم الهاتف</th>
                        <th>رقم البطاقة</th>
                        <th>الحالة</th>
                        <th>إجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Dynamic Workers -->
                </tbody>
            </table>
        </div>
    </main>

    <script src="script.js"></script>
    <script src="dashboard.js"></script> <!-- File will be created later for chart logic -->
</body>
</html>